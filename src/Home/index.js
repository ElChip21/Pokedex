import React from "react";
import SearchComponent from "./components/searchComponent";

const Home = () => {
    return (
        <div>
            <SearchComponent />
        </div>
    );
};

export default Home;
